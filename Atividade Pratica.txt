import  'package:flutter/material.dart';
//importe do material design vai escrever o método main

void main() {
  runaApp(MaterialApp(
    home: Home(),
    debugShowCheckModeBanner: false;
  )); // MaterialApp
}

class Home extends StatefulWidget {
  const Home({ key ? }) : super(key: key);

  override 
  _HomeState createState() => _HomeState();
}

class _HopmeState extends State<Home> {

  @override 
  Widgeet builçd(BuildContext contxt) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Home Pgae"),
        backgroundColor: Colors.orange,
      ), // AppBar,
      body: Container(
        width: double.infinity,
        padding: EdgeInsets.all(16),
        decoration: BoxDecoration(
          border: Border.all(width: 3,color: Colors.amber)
        child:Column(
        
          mainAxisAlignment: MainAxisAlignment.spaceEvenly,
          crossAxisAlignment: CrossAxisAlignment.center,

          children: <widget>[
            Image.asset("imagens/logo.png"),
            Text(
              _gerar,
              textAlign: TextAlign.center,
              style: TextStyle(
              fontSize: 22,
              fontStyle: FontStyle.normal,
              backgroundColor: Colors.orange,
              ), //TextStyle
            ), // Text
            @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Componentes de entrada"),
        backgroundColor: Colors.redAccents,
      ), // AppBar
      body: Column(),
        children:<widget>[
          Padding(
            padding: EdgeInsets.all(30),
            child: TextField(
            child: TextField(
              keyboardType: TextInpuType.email,
              decoration: InputDecoration(labelText: "Digite Seu Email"),
                maxLenght: 30, // Limite de caracteres
                obscured: false, // false = visível true = não visível
                //ao submeter a informação quando clicado no botão
                onSubmitted: (String e){
                  print("Digite Seu Email: " +e);
                },
                controller: _textEditingController,
            ), // TextField
          ), // Padding
            )
              keyboardType: TextInpuType.password,
              decoration: InputDecoration(labelText: "Digite Sua Senha"),
                maxLenght: 10, // Limite de caracteres
                obscured: true, // false = visível true = não visível
                //ao submeter a informação quando clicado no botão
                onSubmitted: (String e){
                  print("Digite Sua Senha: " +e);
                },
                controller: _textEditingController,
            ), // TextField
          ), // Padding
          //ignore: deprecated_member_use
          RaisedButton(
            child: Text("Login"),
            color: Colors.pinkAccent,
            onPressed: (){
              print("Valor foi: " +_textEditingController.text);
            
            ), // RaisedButtom
          ], // <widget>[]
          ), // Column
      ), // Container
    ); // Scaffold
  }